#pragma once
#include <vector>

std::pair < std::pair<int, int>, int > FindMaxCrossingSubarray(std::vector<int>, int, int, int);
std::pair < std::pair<int, int>, int > FindMaximumSubarray(std::vector<int>, int, int);

void TestMaximumSubarrayProblem();